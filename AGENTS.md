# Rules

- NEVER create any commits. Only stage changes (`git add`). Wait for the user to explicitly say "commit" before committing.
- Use conventional commits (e.g., `feat:`, `fix:`, `chore:`, `docs:`)
